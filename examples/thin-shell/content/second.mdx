---
title: A second page.
slug: second
nav: Second
order: 20
date: 2026-08-19
tags: [example]
description: Enough surface to prove links, headings and components render under library composition.
---

A second entry, so the nav has something to navigate and backlinks have something to
resolve.

## Headings get anchors

The heading above is anchored by the same `remarkHeadingAnchors` pass the fork uses — the
engine is the same code either way.

<Callout kind="note" title="This box is the override.">
  `Callout` is a manifest component, so a shell may replace it. This one is rendered by
  `src/ShellCallout.tsx`, not by the engine's stock component — the visible difference is
  `data-kind` on an `aside.shell-callout`. Swap `components.ts` back to stock and this box
  changes shape.
</Callout>

Back to [home](/content/home.mdx).
